    # 左側の重量計の座標（左上、右上、右下、左下）
    left_pts = np.array([
        [275, 435],  # 左上
        [280, 160],   # 右上
        [405, 180],   # 右下
        [395, 455]   # 左下
    ], dtype=np.float32)
    
    # 右側の重量計の座標（左上、右上、右下、左下）
    right_pts = np.array([
        [878, 190],  # 左上
        [818, 440],  # 右上
        [717, 400],  # 右下
        [772, 160]    # 左下
    ], dtype=np.float32)